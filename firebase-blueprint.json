{
  "entities": {
    "User": {
      "title": "User",
      "description": "User profile data including basic identity and role.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "Unique Firebase Auth ID" },
        "email": { "type": "string", "format": "email", "description": "User's email address" },
        "name": { "type": "string", "description": "User's full name" },
        "photoURL": { "type": "string", "format": "uri", "description": "User's profile picture URL" },
        "role": { "type": "string", "enum": ["user", "admin"], "description": "User's access level" },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp when the user first joined" }
      },
      "required": ["uid", "email"]
    },
    "RoomScan": {
      "title": "RoomScan",
      "description": "A record of a room analysis performed by the AI.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "The UID of the user who performed the scan" },
        "category": { "type": "string", "description": "The category of electronics being sought" },
        "roomSize": { "type": "string", "description": "The size of the room" },
        "budget": { "type": "string", "description": "The user's budget preference" },
        "image": { "type": "string", "description": "Base64 encoded image of the room" },
        "analysis": { "type": "object", "description": "The full analysis result from Gemini" },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp when the scan was performed" }
      },
      "required": ["uid", "category", "analysis", "createdAt"]
    }
  },
  "firestore": {
    "/users/{uid}": {
      "schema": "User",
      "description": "Individual user profile documents."
    },
    "/scans/{scanId}": {
      "schema": "RoomScan",
      "description": "Collection of all room scans performed by users."
    }
  }
}
